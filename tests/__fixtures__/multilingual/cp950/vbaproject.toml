# Traditional Chinese (CP950) project fixture for testing encoding detection and handling.
[project]
name = "multilingual-cp950"
target = { type = "xlsm", path = "targets/xlsm" }

[source]
encoding = "cp950"

[src]
NiHao = "src/NiHao.bas"
