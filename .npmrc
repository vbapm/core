# Only install packages that have been published for at least 7 days.
# This mitigates supply-chain attacks where a compromised version is
# published and immediately consumed by CI or developers.
min-release-age=5
