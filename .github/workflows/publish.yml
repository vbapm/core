# Auth: Trusted Publishing: https://docs.npmjs.com/trusted-publishers

name: Publish Package

on:
  workflow_dispatch:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'
      - 'v[0-9]+.[0-9]+.[0-9]+-*'

jobs:

  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:

      - name: Checkout
        uses: actions/checkout@v6

      - name: Download addins from release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release download ${{ github.ref_name }} --pattern "vbapm-win.zip" --dir /tmp
          unzip /tmp/vbapm-win.zip "addins/build/*" -d ./

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22.x
          registry-url: 'https://registry.npmjs.org/'

      - name: Upgrade npm (required for trusted publishing)
        run: npm install -g npm@latest

      - name: Install
        run: npm ci
        env:
          CI: true

      - name: Typecheck
        run: npm run typecheck

      - name: Build
        run: npm run build:lib

      - name: Determine npm tag
        id: npm-tag
        run: |
          VERSION=$(jq -r '.version' package.json)
          if [[ "$VERSION" == *-* ]]; then
            echo "tag=next" >> $GITHUB_OUTPUT
          else
            echo "tag=latest" >> $GITHUB_OUTPUT
          fi

      - name: Publish to npm
        run: npm publish --provenance --tag ${{ steps.npm-tag.outputs.tag }}

      - name: Check git status
        run: git status && git diff
